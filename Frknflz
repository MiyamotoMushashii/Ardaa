<!doctype html>
<html lang="en"> 
 <head> 
  <meta charset="UTF-8"> 
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
  <title>Furkan &amp; Arda</title> 
  <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #000; /* Siyah arka plan */
            font-family: Arial, sans-serif;
            overflow: hidden;
        }
        /* Galaksi Arka Planı */
        .galaxy-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://source.unsplash.com/1600x900/?galaxy') no-repeat center center;
            background-size: cover;
            z-index: -1;
        }
        /* Sayfa İçeriği */
        .content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
            color: white;
        }
        .title {
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .video-container {
            position: relative;
            display: inline-block;
            cursor: pointer;
        }
        .video-container img {
            width: 100%;
            max-width: 500px;
            border-radius: 10px;
        }
        .video-container .play-button {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 80px;
            height: 80px;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .video-container .play-button::before {
            content: '';
            display: inline-block;
            border-style: solid;
            border-width: 15px 0 15px 25px;
            border-color: transparent transparent transparent white;
            margin-left: 5px;
        }
        /* Yıldızlar */
        .star {
            position: absolute;
            border-radius: 50%;
            background: white;
            opacity: 0.5;
            animation: moveStars 60s linear infinite;
        }
        /* Yıldızların hareketi */
        @keyframes moveStars {
            0% { transform: translateY(0); }
            100% { transform: translateY(100vh); }
        }
    </style> 
 </head> 
 <body> 
  <div class="galaxy-background"></div> 
  <div class="content"> 
   <div class="title">
    Furkan &amp; Arda
   </div> 
   <div class="video-container" onclick="window.open('https://youtu.be/XKvF9w9YRCc', '_blank')"> 
    <img src="https://img.youtube.com/vi/XKvF9w9YRCc/maxresdefault.jpg" alt="YouTube Kapak Resmi"> 
    <div class="play-button"></div> 
   </div> 
  </div> 
  <script>
        // Yıldızları rastgele yerleştirme
        function generateStars() {
            for (let i = 0; i < 100; i++) {
                let star = document.createElement("div");
                star.classList.add("star");
                // Rastgele boyut ve konum
                let size = Math.random() * 3 + 1 + 'px';
                let left = Math.random() * window.innerWidth + 'px';
                let top = Math.random() * window.innerHeight + 'px';
                let duration = Math.random() * 10 + 60 + 's';
                
                star.style.width = size;
                star.style.height = size;
                star.style.left = left;
                star.style.top = top;
                star.style.animationDuration = duration;

                document.body.appendChild(star);
            }
        }

        // Yıldızları başlat
        generateStars();
    </script> 
 </body>
</html>
